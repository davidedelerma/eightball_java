<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.eightball">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Eightball"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
          </activity>
        <activity
            android:name=".AnswerActivity"
            android:label="@string/answer"
            android:parentActivityName=".Eightball">

        </activity>

        <activity
            android:name=".AnswerActivityButton2"
            android:label="@string/answer2"
            android:parentActivityName=".Eightball">

        </activity>

        <activity
            android:name=".AnswerFromAshleighActivity"
            android:label="@string/funanswer"
            android:parentActivityName=".Eightball">

        </activity>
    </application>

</manifest>
